{{#unless regular}}
  <div class="col-xs-8">
    <h2>Users</h2>
    <table id="users_table" class="table">
      <thead>
        <tr>
          <th>Email</th>
          <th>Role</th>
          <th>Expected Calories</th>
          <th colspan="5"></th>
        </tr>
      </thead>
      <tbody>
        {{#each model as |user|}}
          <tr>
            <td>{{user.email}}</td>
            <td>{{user.role}}</td>
            <td>{{if user.expected_calories user.expected_calories '-'}}</td>
            <td><a href="/users/edit/{{user.id}}" class="glyphicon glyphicon-pencil"></a></td>
            <td><a {{action 'deleteAfterConfirm' user}} class="glyphicon glyphicon-trash"></a></td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
{{/unless}}
